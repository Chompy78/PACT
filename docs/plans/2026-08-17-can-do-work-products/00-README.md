# PACT — "Can Do" work products

Generated to be **folded in** by the single writer, per the task board rules
("Agents: *output* new items in this format for the human to fold in — don't
append directly"). Nothing here has been committed, and no repo/live-Supabase
state was touched — I don't have that access.

## What's inside

| File | Task it satisfies | What it is | How to use it |
|---|---|---|---|
| `01-drawback-ap-double-count-decision.md` | `fix/drawback-ap-double-count` (NEXT) | The `D-GH-<date>-drawback-ap-double-count` record the task asks you to log | Paste into `decisions/2026/`, date it, then implement model (a) as specified |
| `02-dm-edit-events-decision.md` | `feat/dm-edit-events` (NEXT) | The decision record consolidating every owner ruling already made in the entry | Paste into `decisions/2026/`; it is the "open questions answered in a decision record" half of *Done when* |
| `03-campaign-ap-budget-enforce-spec.md` | `feat/campaign-ap-budget-enforce` (NEXT) | A build-ready implementation spec + differential test plan (design was already "fully settled") | Hand straight to the coding agent as the task brief |
| `04-pace-curve-terminology-annotation-pack.md` | `docs/pace-curve-terminology` (NEXT) | Paste-ready, dated correction notes for all five named sites | Re-grep first (line numbers drift), then paste each note *beside* the original — never rewrite |

## Why only these four are "can do"
Every other open task needs at least one of: (a) the actual repo files
(`js/engine.js`, the three tools, `sql/*`), (b) a live signed-in Supabase
session / the advisor, or (c) an owner-only decision that isn't yet made in the
text. Those are in the "can do part" (planning) and "can't do" buckets in the
chat message that produced this zip.

## Scope discipline applied
- Australian English throughout.
- Canon respected — no invented mechanics; every figure traces to the task text
  or the decision record it cites.
- These are **drafts for folding in**, not commits. Dates are left as
  `<date>` where the board's naming convention wants the fold-in date.
