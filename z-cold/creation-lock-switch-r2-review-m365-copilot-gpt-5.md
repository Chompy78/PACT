M365 Copilot (GPT-5 reasoning model, default)

- **[blocking | high confidence] The unlock suppression mechanism is not specified well enough to implement or test consistently.** The plan says `creationUnlocked` suppresses automatic locking “until re-armed”, but never defines the complete set of events or state transitions that re-arm it. The later test “unlocking then re-finalising ends locked” establishes that `creationLocked` overrides an unlock, but it does not establish whether a subsequent `creationLockConfig`, `campaignBound`, threshold change, spend change, or removal from the DM unlock list re-arms automatic locking. This is now a core replay rule, not a UI detail. Define an explicit replay state machine or precedence rule covering every relevant event, then add fixtures for each re-arm path.

- **[blocking | high confidence] DM unlock materialisation is one-way, while the settings model implies a reversible list.** The client appends `creationUnlocked` when the character ID is present in the DM’s unlocked list, but the plan specifies no event when the DM removes that ID. Because the log remains unlocked and automatic locking is suppressed, removing a character from the list appears to do nothing. If the list is intended as a command queue or permanent grant, say so and define how entries are consumed or retained. If it represents current desired state, reconciliation needs a corresponding re-lock/re-arm transition and an equality rule that will not append repeatedly.

- **[blocking | high confidence] The campaign-marker self-healing rule is too weak to resist undo under an append-only log.** Reconciliation only re-appends `campaignBound` when the log “lacks it”. If undo is implemented by removing the last event, this may work for a trailing marker, but once later events exist the marker is no longer the last event and cannot ordinarily be popped without also removing those later events. More importantly, if an unbind, tombstone, or effective-state override exists now or later, mere historical presence is not equivalent to effective membership. The guard must compare effective folded campaign-binding state and the authoritative membership ID, not simple event existence. The plan also needs to state what happens when a character leaves or changes campaigns.

- **[moderate | high confidence] Campaign identity and cross-campaign contamination are unspecified.** `campaignBound` is described as a marker and campaign settings are materialised into the player log, but the plan does not say whether binding/config events carry a campaign ID or how stale events from a former campaign are handled. A character moved between campaigns could retain the former campaign’s threshold, explicit unlock suppression, or binding marker. Add movement and removal semantics, including authority when the character’s membership record and historical log disagree.

- **[moderate | high confidence] The prose still contains a misleading contradiction about the campaign gate.** One shipped bullet says automatic lock “still requires `campaignBound`”, then immediately says solo opt-in is honoured without `campaignBound`. The intended rule seems coherent, but the absolute first sentence is false. State the actual predicate directly, for example: automatic locking is armed when `auto === true`, or when `auto` is unset and a campaign binding exists; `auto === false` disarms it. Also state where threshold and unlock suppression enter that predicate.

- **[moderate | high confidence] The source-of-truth and conflict policy between DM settings and player-authored log events is incomplete.** Materialisation is last-write-wins, yet players control their own append-only logs under the honour-system scope. A player can potentially append a differing `creationLockConfig` after materialisation, not merely undo finalisation. The next online load will correct it only if reconciliation compares effective settings against the latest config, but offline operation may use the player’s override indefinitely. The honour-system limitation is defensible, but the plan should explicitly include config tampering and offline divergence in that limitation rather than discussing only undoing purchases/finalise.

- **[moderate | high confidence] The reconciliation algorithm lacks concurrency and write-failure requirements.** “Append each event if different” is insufficient when two tabs or two tools load concurrently: both can observe the same stale tail and append duplicate config, unlock, or binding events. Equality guards do not prevent this race. Duplicates may be semantically harmless but cause log churn, and partial success across several appends can leave an intermediate state. Define whether duplicate semantic events are accepted, add stable idempotency keys or transactional/CAS behaviour if available, and test concurrent loads plus retry after partial failure.

- **[moderate | high confidence] The DM unlock acknowledgement model can generate repeated unlocks after player re-finalisation.** If a character remains in the DM’s unlocked list, the player can append `creationLocked`; on the next load reconciliation may see that the character is listed and is no longer effectively unlocked, then append another `creationUnlocked`. This conflicts with the test “unlocking then re-finalising ends locked” unless the list entry is consumed, versioned, acknowledged, or the equality check looks only for historical presence. Each interpretation has different behaviour. Specify command identity and acknowledgement semantics, then test re-finalisation followed by another online load.

- **[moderate | high confidence] State-indicator derivation is an unstated dependency.** The plan verifies that `compute()` exposes no lock state and says the lock map lives on the build, but the tools must display “still in creation” versus “locked”. It does not identify a stable engine API for deriving the current effective state, including unlock suppression and reversible threshold behaviour. Reading engine internals independently in three tools risks recreating the divergence that caused the bug. Add a shared engine-level query or documented build field while preserving the public API shape, and test all three tools against it.

- **[moderate | high confidence] The backfill selection rule is broader than the safety proof.** The implementation says to append `campaignBound` for characters “already in a campaign”, while the dry run covers four current characters. Define exact eligibility from authoritative membership, exclude already-effectively-bound characters, record the campaign identity if applicable, and make the production operation idempotent. The simulated post-marker purchase should also use a trait and tier that distinguish pricing and should prove the expected threshold has already been exceeded; otherwise “prices expensive” may fail for legitimate reasons.

- **[moderate | high confidence] The fixture matrix omits several newly critical ordering cases.** Add at least: unlock while over threshold, unlock then unchanged config materialisation, unlock then changed config, unlock then `campaignBound`, unlock then explicit finalise then reload while still on the DM list, removal from the DM unlock list, simultaneous explicit and automatic lock, campaign departure/transfer, threshold `0`, threshold `null`, negative or non-numeric thresholds, and spend oscillating across the threshold before and after suppression. The current fixtures cover the original pricing issue well but do not fully pin the revised unlock/materialisation semantics.

- **[moderate | high confidence] Input validation and malformed historical events are not addressed.** The event permits `threshold: number|null`, but the accepted range, integer/decimal policy, treatment of zero/negative/NaN values, and behaviour for malformed events are absent. Because logs are durable and browser-authored, replay must fail safely and deterministically. Define validation at both write and replay boundaries and add malformed-event fixtures.

- **[minor | high confidence] Terminology remains internally inconsistent.** “Finalise”, “explicit lock”, “automatic lock”, “unlocked”, “still in creation”, “suppressed”, and “re-armed” describe related but non-equivalent states. This is no longer merely a documentation polish issue because UI wording and test assertions depend on the distinctions. A short state glossary plus a transition diagram would materially reduce implementation drift.

- **[solid] The pricing goal, frozen per-purchase stamps, strict-greater-than threshold rule, tier-3 test requirement, and future-only pricing effect are clearly stated and objectively testable.** The plan correctly avoids relying on tier 1, where the two prices coincide.

- **[solid] The strengthened production dry run is appropriate.** Comparing the complete folded build, every per-purchase stamp, and full `compute()` output before and after a trailing backfill marker is substantially stronger than totals-only comparison. Requiring zero diff before touching live data is an appropriate release gate.

- **[solid] Keeping campaign policy in replayable log events is coherent with event sourcing.** It preserves historical replay better than passing today’s campaign settings directly into `compute()`, provided the unresolved reconciliation, campaign identity, and authority rules above are made explicit.

- **[solid] The honour-system v1 scope is defensible as a product decision, but only if its boundary is described accurately.** It does not prevent a determined player from changing or withholding locally authored policy events, especially offline. That does not defeat the pricing mechanism for cooperative play, but it means the plan achieves reliable workflow behaviour rather than enforcement.

- **[overall verdict | blocking | high confidence] Revision 2 resolves the major round-1 contradictions, but it is not yet implementation-complete.** The central remaining blocker is the absence of a precise state machine for unlock suppression/re-arming and a command/state model for the DM unlock list. Those omissions directly affect replay determinism, reconciliation idempotence, and the stated “unlock then re-finalise” behaviour. Resolve those rules before implementing the remaining materialisation and UI work.
