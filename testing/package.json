{
  "name": "pact-testing",
  "private": true,
  "description": "Dev/CI-only test tooling for PACT. Not part of the app — the app itself needs no npm install.",
  "scripts": {
    "e2e:character": "node scripts/random-manual-e2e.mjs",
    "log-fuzz": "node scripts/log-fuzz.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1",
    "supabase": "^2.109.1"
  }
}
